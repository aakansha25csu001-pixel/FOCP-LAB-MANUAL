#include<iostream>
#include<fstream>
using namespace std;
int main (){
   
    ifstream fin ("aakansha.txt");
    if(!fin.is_open()){
        cout<<"\n issue with file";
        return 0;
    }
    string line;
   while(fin>>line){
    //while(getline(fin,line)){
  
    cout<<line<<endl;
   
    }
     
    fin.close();
}
